# Research: Publication Engine

The publication engine deliberately does not call GitHub. It is a deterministic gate over evidence gathered by the orchestrator or future providers. This keeps the first publication runtime provider-neutral and testable without network state.
