# Research: Project Configuration

Spec009 uses JSON for executable configuration to remain standard-library only. YAML examples remain documentation, but runtime parsing is explicit JSON until a parser dependency is introduced by policy.
