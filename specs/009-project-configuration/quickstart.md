# Quickstart: Project Configuration

```powershell
python -m ados config validate --config tests/fixtures/project-config.valid.json
```

The validated output includes project facts and the derived immutable Execution Policy.
