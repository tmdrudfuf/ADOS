# Research: Exact HEAD Gate

The gate is deliberately local and narrow in Spec006. Remote branch and PR HEAD comparison belongs to the publication engine. This module only proves the foundational ADOS equation:

```text
Approved Review SHA = Validated SHA = Current HEAD
```
