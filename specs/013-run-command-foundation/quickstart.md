# Quickstart: ADOS Run

```powershell
python -m ados run --project C:\path\to\project --feature "Add a focused feature"
python -m ados run --project C:\path\to\project --spec 084 --feature "Add a focused feature" --dry-run --json
```

Successful real runs stop at `READY_FOR_IMPLEMENTATION`.
