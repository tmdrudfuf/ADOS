# Quickstart: ADOS Status

```powershell
python -m ados status --project C:\path\to\project --config C:\path\to\project-config.json
python -m ados status C:\path\to\project --config C:\path\to\project-config.json --json
```

Status is diagnostic-only. It does not run validation, review, publication, or cleanup.
